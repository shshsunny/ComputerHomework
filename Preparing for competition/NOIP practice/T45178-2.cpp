#include<iostream>

using namespace std;
int solve(int a, int b, int c, int x, int y)
{
}
int main()
{
	int a, b, c, x, y;
	cin >> a >> b >> c >> x >> y;
	cout << solve(a, b, c, x, y) <<endl;
	return 0;
}
